{
  "name" : "Haskell Dev",
  "image" : "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "postCreateCommand" : "apt-get update && apt-get install -y ghc cabak-install && echo 'export PATH=$PATH:/usr/bin' >> ~/.bashrc && ghc --version",

  "remoteUser" : "vscode"
  }
}
